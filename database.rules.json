{
  "rules": {
    "members": {
      "$member_uid": {
        ".read": "auth != null",

        // Only the member is able to update his/her profile
        ".write": "auth != null && auth.uid === $member_uid"
      }
    },

    "prayers": {
      // List of prayers
      ".read": "auth != null",

      "$prayer_id": {
        // New prayer
        ".write": "auth != null"
      }
    }
  }
}